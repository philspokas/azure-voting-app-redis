apiVersion: v1
kind: Service
metadata:
    name: "demoaks1-93be"
    labels:
        app: "demoaks1-93be"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "demoaks1-93be"